beforeEach(() => {
  jest.clearAllMocks();
  jest.resetAllMocks();
});

describe("QuotePostUseCase Test", () => {
  it("Should create a Quote Post", async () => {
    console.log("testing...");
    expect(true).toBe(true);
  });

  it("Should validate a Quote Post before creating", async () => {
    console.log("testing...");
    expect(true).toBe(true);
  });
});
