beforeEach(() => {
  jest.clearAllMocks();
  jest.resetAllMocks();
});

describe("GetUserProfileUseCase Test", () => {
  it("Should get User Profile", async () => {
    console.log("testing...");
    expect(true).toBe(true);
  });
});
